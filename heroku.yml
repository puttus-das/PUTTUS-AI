services:
  puttus-ai:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./session:/app/session
      - ./data:/app/data
      - ./temp:/app/temp
      
